{
  "name": "quiz-web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "homepage": "https://stanisraja.github.io/quiz-web",
  "dependencies": {
    "axios": "^0.21.1",
    "cors": "^2.8.5",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.25.1",
    "react-scripts": "^4.0.3"
  },
  "scripts": {
    "start": "SET NODE_OPTIONS=--openssl-legacy-provider && react-scripts --openssl-legacy-provider start",
    "build": "SET NODE_OPTIONS=--openssl-legacy-provider && react-scripts build",
    "test": "SET NODE_OPTIONS=--openssl-legacy-provider && react-scripts test",
    "eject": "SET NODE_OPTIONS=--openssl-legacy-provider && react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "main -d build -x node_modules -x src -x public"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3",
    "react-app-rewired": "^2.2.1"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
